apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cms-core.fullname" . }}-test-connection"
  labels:
    {{- include "cms-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cms-core.fullname" . }}:{{ .Values.frontend.port }}']
  restartPolicy: Never
