# Default values for identity.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: knkcms.azurecr.io/cms-identity
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

configs:
  app:
    key: -JaNdRgUkXn2r5u8x/A?D(G+KbPeShVm
    url: http://127.0.0.1:8080
    redirectUri: https://knk.com
    whitelisted_redirect_uris:
      - knk.com
      - id.knkcms.io
  courier:
    sender: knkCMS <noreply@knkcms.io>
    nats:
      url: tcp://127.0.0.1:4222
    driver:
      type: ses
      config:
        key: AKIA5NI7XVVPTM2X36OF
        secret: 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
        region: eu-west-1
  sentry:
    url: https://0ca5018b0f1a43ad9f8408c3d1356736@sentry.prod.int-tools.knkcms.io/2
    environment: development
  server:
    api:
      addr: 0.0.0.0:8001
      timeout: 1s
    ui:
      addr: 0.0.0.0:8080
      timeout: 60s
    grpc:
      addr: 0.0.0.0:9001
      timeout: 1s
  repository:
    type: sqlite3
    config:
      file: var/sqlite/db.db
  session:
    cookieDomain: "*.knkcms.io"
    cookieName: "x-session-id"
    driver: redis
    config:
      host: 127.0.0.1
      password: "sOmE_sEcUrE_pAsS"
      port: 6379
      database: 1
  connectors: []
  #  - type: microsoft
  #    id: microsoft
  #    name: Microsoft
  #    config:
  #      clientID: 83bf29f3-7513-4cf9-af1e-5481ae01b22f
  #      clientSecret: t0i7Q~k28Ms3tDA4Eh-wDACMaIbPgwLG3ZTH2
  #      redirectURI: http://127.0.0.1:8080/callback
  #  - type: github
  #    id: github
  #    name: GitHub
  #    config:
  #      clientID: cf9ff4c5fab0314805ad
  #      clientSecret: 5fcdf17d75260ca7ca960633d2be6b10a21314ce
  #      redirectURI: http://127.0.0.1:8080/callback