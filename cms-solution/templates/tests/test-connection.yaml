apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cms-solution.fullname" . }}-test-connection"
  labels:
    {{- include "cms-solution.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cms-solution.fullname" . }}:{{ .Values.area.service.port }}','{{ include "cms-solution.fullname" . }}:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
