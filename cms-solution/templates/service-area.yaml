apiVersion: v1
kind: Service
metadata:
  name: "area-service" # {{ include "cms-solution.fullname" . }}
  labels:
    {{- include "cms-solution.labels" . | nindent 4 }}
spec:
  type: {{ .Values.area.service.type }}
  ports:
    - port: {{ .Values.area.service.port }}
      targetPort: http
      protocol: TCP
      name: areaservice
  selector: 
    {{- include "cms-solution.selectorLabels" . | nindent 4 }}
