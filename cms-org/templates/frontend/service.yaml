apiVersion: v1
kind: Service
metadata:
  name: {{ include "cms-org.fullname" . }}-frontend
  labels:
    {{- include "cms-org.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cms-org.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
