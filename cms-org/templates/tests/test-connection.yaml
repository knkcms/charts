apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cms-org.fullname" . }}-test-connection"
  labels:
    {{- include "cms-org.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cms-org.fullname"-backend . }}:{{ .Values.backend.service.port }}']
  restartPolicy: Never
